<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bjts.board.dao.login.LoginDao">

	<select id="valueCheckId" resultType="String">
		SELECT USERID FROM MEMBER WHERE USERID = #{value}
	</select>
	
	<select id="valueCheckPassword" resultType="String">
		SELECT USERPASSWORD FROM MEMBER WHERE USERID = #{value}
	</select>
	
	<select id="getValueNickname" resultType="String">
		SELECT USERNICKNAME FROM MEMBER WHERE USERID = #{value}
	</select>
  
</mapper>
